# Feature Specification: LGPD Deletion Request

## Summary

Implement a real LGPD deletion request flow with public intake, persistence, and administrative review.

## Requirements

- The portal must accept public deletion requests tied to CPF and contact data.
- The backend must persist requests with status and review metadata.
- The admin panel must list and update request status.
- The implementation must include automated backend tests.
