# Plan: Athlete Export CSV

## Scope
- Add CSV generation in the athletes service and a protected controller route.
- Add an admin UI action that downloads the CSV using the existing admin token.
- Validate backend tests and app builds.
