# Tasks: 061-admin-content-guidance

- [ ] Update `.specify/feature.json` to point to feature 061.
- [ ] Simplify `Patrocinio e midia` navigation in `frontend/app/admin/admin-shell.tsx`.
- [ ] Migrate `/admin/patrocinio` to the shared admin component system and add explanatory header copy.
- [ ] Migrate `/admin/backdrop` to the shared admin component system and add explanatory header copy.
- [ ] Migrate `/admin/midia` to the shared admin component system and add explanatory header copy.
- [ ] Migrate `/admin/midia/nova` to the shared admin component system and add explanatory header copy.
- [ ] Update admin E2E coverage for the new navigation structure and guidance copy.
- [ ] Run `frontend npm run test:e2e -- --reporter=list`.
- [ ] Run `frontend npm run build`.
